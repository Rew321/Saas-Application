
# Django Tenants
Django Tenants is a multi-tenancy application that uses Postgres schemas to separate tenants.
It uses the concept of a "tenant" to identify a group of users and data that belong together.
The application is built using Django and uses the Django ORM to interact with the database.

## Installation
To install Django Tenants, add the following to your `requirements.txt` file:


